name: Test Compression
on:
  push:
jobs:
  build:
    name: Build Test
    runs-on: ubuntu-latest
    container: debian:stable-slim
    steps:
    - uses: actions/checkout@v2
    - uses: actions/github-script@v5
      with:
        script: |
          console.log("process.env['RUNNER_TEMP']="+process.env['RUNNER_TEMP']);
          console.log("process.cwd()="+process.cwd());
          console.log("Home directory = "+require('os').homedir());
          console.log(process.env['github.workspace']);
          console.log(process.env['GITHUB_WORKSPACE']);
    - uses: actions/cache@v3
      id: cache
      with:
        path: ${{ github.workspace }}/node_modules
        key: ${{ runner.os }}-build1-${{ env.cache-name }}-${{ hashFiles('**/package-lock.json') }}
        restore-keys: ${{ runner.os }}-build1-${{ env.cache-name }}-${{ hashFiles('**/package-lock.json') }}
    - run: npm install